== To Do

- deploy (load new fixtures), kick tires in production
- set up real accounts, add wines/tastings


== Future 

- fix photo
- add account functionality for users, user groups, private vs public on wines/tastings